# Generated by ensureTenantTraefikRouter for tenant "globex" — do not hand-edit,
# re-provisioning regenerates this file. See traefik-router.ts for why this
# exists (literal Host() rules are required for on-demand ACME certs).
http:
  routers:
    app-globex-tenant:
      rule: 'Host(`app.globex.techhanker.com`)'
      service: k8s-ingress
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      priority: 15

    admin-globex-tenant:
      rule: 'Host(`admin.globex.techhanker.com`)'
      service: web-service
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      priority: 15
