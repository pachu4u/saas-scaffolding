http:
  routers:
    keycloak:
      rule: 'Host(`auth.${domain_name}`)'
      entryPoints:
        - web
      service: keycloak
      priority: 100

  services:
    keycloak:
      loadBalancer:
        servers:
          - url: 'http://keycloak:8080'
