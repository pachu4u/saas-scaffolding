# Generated by ensureTenantTraefikRouter for tenant "acme" — do not hand-edit,
# re-provisioning regenerates this file. See traefik-router.ts for why this
# exists (literal Host() rules are required for on-demand ACME certs).
http:
  routers:
    app-acme-tenant:
      rule: 'Host(`app.acme.techhanker.com`)'
      service: k8s-ingress
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      priority: 15

    admin-acme-tenant:
      rule: 'Host(`admin.acme.techhanker.com`)'
      service: web-service
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      priority: 15
