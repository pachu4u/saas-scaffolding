# Generated by ensureTenantTraefikRouter for tenant "demo" — do not hand-edit,
# re-provisioning regenerates this file. See traefik-router.ts for why this
# exists (literal Host() rules are required for on-demand ACME certs).
http:
  routers:
    app-demo-tenant:
      rule: 'Host(`app.demo.techhanker.com`)'
      service: k8s-ingress
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      priority: 15

    admin-demo-tenant:
      rule: 'Host(`admin.demo.techhanker.com`)'
      service: web-service
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      priority: 15
